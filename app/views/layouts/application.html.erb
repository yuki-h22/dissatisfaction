<!DOCTYPE html>
<html>
  <head>
    <title>Dissatisfaction</title>
    <meta name="description", content="I want to share my worries.Or share your worries.">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body class="body">
    <header class="header">
      <div class="header__bar">
        <h2 class="header__bar__title">
        <%= link_to root_path, class:"header__bar__title__text border-none" do %>
          SYW
        <% end %>
        </h2>
         <% if user_signed_in? %>
          <div class="user__name grid">
            <span><%= current_user.name%>
              <ul class="user__name__info">
                <li>
                  <%= link_to "投稿する", new_bitch_path, class: "top-btn border-none" %>
                </li>
                <li>
                  <%= link_to "ログアウト", destroy_user_session_path, method: :delete, class: "top-btn border-none" %>
                </li>
              </ul>
            </span>
          </div>
        <% else %>
          <div class="grid">
            <%= link_to "ログイン", new_user_session_path, class: "top_btn border-none" %>
            <%= link_to "新規登録", new_user_registration_path, class: "top_btn border-none" %>
          </div>
        <% end %>
      </div>
    </header>
    <%= yield %>
    <footer class="footer">
      <div>
        <div class="descriotion">
          <div class="description__title border-none">
            <a>このアプリについて</a>
          </div>
        </div>
      </div>
    </footer>
  </body>
</html>
